{
  "enabled": true,
  "taskFile": "tasks/current-task.json",
  "stateFile": "tasks/executor-state.json",
  "handlers": {
    "example-task-type": {
      "match": "example-task-type",
      "plan": [
        "inspect current task state",
        "identify the next concrete action",
        "execute the action",
        "capture output or evidence",
        "write progress back to the task ledger"
      ]
    }
  },
  "notes": "Template executor handler definitions for public use. Copy to executor-config.json and replace example handlers with your own task types. Preferred extension path is task-registry.json + task-types.json; handlers remain for backward compatibility."
}
